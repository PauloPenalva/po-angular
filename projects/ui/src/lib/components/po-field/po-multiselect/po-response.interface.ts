/**
 * @docsPrivate
 *
 * @description
 *
 * Interface utilizada no PoMultiselectFilterService para o retorno no padrão API TOTVS.
 * Verificar necessidade   ***.
 */
export interface PoResponse {
  items: Array<any>;
}
